name: Secret probe (temporary)

on:
  workflow_dispatch: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Check SATIS_DISPATCH_TOKEN delivery
        run: |
          if [ -n "${{ secrets.SATIS_DISPATCH_TOKEN }}" ]; then
            echo "RESULT=SECRET_PRESENT"
          else
            echo "RESULT=SECRET_MISSING"
          fi
